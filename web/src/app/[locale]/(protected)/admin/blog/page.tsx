import { AdminPostsPage } from "@/components/blog/admin/AdminPostsPage";

/** The admin's blog table (2026-09-19). The table itself is shared with the guide's tab. */
export default function AdminBlogPage() {
  return <AdminPostsPage kind="Blog" />;
}
