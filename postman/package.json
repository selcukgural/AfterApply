{
  "name": "ekariyerim-postman",
  "private": true,
  "version": "1.0.0",
  "description": "Generates postman/collection.json from the API's build-time OpenAPI document and (in CI) publishes it to Postman Cloud. See README.md \u2014 nothing under this directory except environments/*.json is hand-edited.",
  "scripts": {
    "generate": "node scripts/generate-collection.js",
    "publish": "node scripts/publish-collection.js",
    "publish:environments": "node scripts/publish-environments.js"
  },
  "dependencies": {
    "openapi-to-postmanv2": "6.3.3"
  },
  "overrides": {
    "js-yaml": "4.3.1",
    "uuid": "11.1.1",
    "yaml": "1.10.3",
    "@faker-js/faker": "file:faker-stub"
  }
}
